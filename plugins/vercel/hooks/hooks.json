{
  "hooks": [
    {
      "event": "pre-commit",
      "command": "npx vercel build --prod --dry-run",
      "description": "Validate the Vercel build configuration before committing changes",
      "enabled": false
    },
    {
      "event": "pre-push",
      "command": "npx vercel build",
      "description": "Run a full Vercel build locally before pushing to ensure the deployment will succeed",
      "enabled": false
    },
    {
      "event": "post-deploy",
      "command": "scripts/deploy.sh",
      "description": "Execute post-deployment tasks such as cache warming and health checks",
      "enabled": true
    },
    {
      "event": "file-change",
      "pattern": "vercel.json",
      "command": "npx ajv validate -s https://openapi.vercel.sh/vercel.json -d vercel.json",
      "description": "Validate vercel.json schema whenever it is modified",
      "enabled": false
    },
    {
      "event": "file-change",
      "pattern": "api/**/*.ts",
      "command": "npx tsc --noEmit --project tsconfig.json",
      "description": "Type-check API route handlers when they are modified",
      "enabled": true
    }
  ]
}
