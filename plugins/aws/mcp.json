{
  "mcpServers": {
    "aws": {
      "command": "npx",
      "args": ["-y", "@awslabs/mcp-server-aws"],
      "description": "AWS Labs MCP server — interact with AWS services (S3, Lambda, DynamoDB, CloudFormation, CloudWatch, IAM, SSM, CDK) directly from Cursor",
      "env": {
        "AWS_PROFILE": "${AWS_PROFILE:-default}",
        "AWS_REGION": "${AWS_REGION:-us-east-1}",
        "AWS_MCP_ALLOW_WRITE": "${AWS_MCP_ALLOW_WRITE:-false}"
      },
      "tools": [
        {
          "name": "s3_list_buckets",
          "description": "List all S3 buckets in the account"
        },
        {
          "name": "s3_get_object",
          "description": "Get an object from an S3 bucket"
        },
        {
          "name": "s3_put_object",
          "description": "Upload an object to an S3 bucket"
        },
        {
          "name": "lambda_list_functions",
          "description": "List all Lambda functions in the account"
        },
        {
          "name": "lambda_invoke",
          "description": "Invoke a Lambda function with a JSON payload"
        },
        {
          "name": "lambda_get_function",
          "description": "Get configuration details for a Lambda function"
        },
        {
          "name": "dynamodb_list_tables",
          "description": "List all DynamoDB tables"
        },
        {
          "name": "dynamodb_query",
          "description": "Query items from a DynamoDB table"
        },
        {
          "name": "dynamodb_describe_table",
          "description": "Get the schema and configuration of a DynamoDB table"
        },
        {
          "name": "cloudformation_list_stacks",
          "description": "List all CloudFormation stacks and their statuses"
        },
        {
          "name": "cloudformation_describe_stack",
          "description": "Get details, outputs, and parameters for a CloudFormation stack"
        },
        {
          "name": "cloudwatch_get_log_events",
          "description": "Get log events from a CloudWatch Logs log group"
        },
        {
          "name": "cloudwatch_describe_alarms",
          "description": "List CloudWatch alarms and their current states"
        },
        {
          "name": "iam_list_roles",
          "description": "List IAM roles in the account"
        },
        {
          "name": "iam_get_role_policy",
          "description": "Get the inline or attached policies for an IAM role"
        },
        {
          "name": "ssm_get_parameter",
          "description": "Get a parameter value from SSM Parameter Store"
        },
        {
          "name": "sts_get_caller_identity",
          "description": "Get the current AWS caller identity (account, ARN, user ID)"
        }
      ]
    }
  }
}
