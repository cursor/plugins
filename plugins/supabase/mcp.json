{
  "$schema": "https://cursor.com/schemas/mcp.json",
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase@latest",
        "--access-token",
        "${SUPABASE_ACCESS_TOKEN}"
      ],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
      },
      "description": "Supabase MCP server — manage projects, run queries, manage database schemas, and interact with Supabase services directly from the editor",
      "documentationUrl": "https://github.com/supabase-community/supabase-mcp"
    }
  }
}
