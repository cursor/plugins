{
  "$schema": "https://cursor.com/schemas/mcp.json",
  "mcpServers": {
    "stripe": {
      "command": "npx",
      "args": ["-y", "@stripe/mcp", "--tools=all"],
      "description": "Stripe MCP server for AI-powered payment operations",
      "env": {
        "STRIPE_SECRET_KEY": "${STRIPE_SECRET_KEY}"
      }
    }
  }
}
