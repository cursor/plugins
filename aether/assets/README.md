Add `avatar.png` here (128×128 or 256×256 recommended) for the plugin marketplace icon.

The `plugin.json` references `assets/avatar.png`. If omitted, the plugin may still install but won't display an icon in the marketplace.
