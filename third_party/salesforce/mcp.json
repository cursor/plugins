{
  "mcpServers": {
    "salesforce": {
      "type": "http",
      "url": "${SALESFORCE_MCP_URL}",
      "auth": {
        "CLIENT_ID": "${SALESFORCE_CLIENT_ID}",
        "scopes": ["mcp_api", "refresh_token"]
      }
    }
  }
}
