# Changelog

## 1.0.1

- Renamed the plugin from `mida` to `mida-abtesting` so it is not confused
  with unrelated projects that share the `mida` prefix.

## 1.0.0

- Initial release: Mida MCP server (hosted, OAuth) plus the `mida` and
  `mida-experiments` skills.
